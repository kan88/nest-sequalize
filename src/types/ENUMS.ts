export const enum E_STATUS {
  CREATE = 0,
  APPROVE = 1,
  DECLINE = 2,
  ARCHIVE = 3,
  EMPTY_FILTER = 9,
}

export const enum E_SERVICE {
  ADMINISTRATOR = 0,
}

export const enum E_ROLE {
  VIEW = 0,
  READ = 1,
  CREATE = 2,
  EDIT = 3,
  FULL = 4,
}
